#include <stdio.h>
#include <math.h>

int main() {
    int q, n;
    scanf("%d", &q);
    for (int i = 1; i <= count; i++) {
        scanf("%d", &n);
        printf("%.0lf", pow(2, n));
    }
    return 0;
}